
memento.folder = "./keys"
datafolder = "./data/"${PORT}
keyfolder = ${memento.folder}

  akka {
    io {
      tcp {
        direct-buffer-size = 1536 KiB
        trace-logging = off
      }
    }
  }


include "node.conf"