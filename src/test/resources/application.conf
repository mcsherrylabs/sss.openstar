DBStorageTest {
  driver = "org.hsqldb.jdbc.JDBCDriver"
  connection = "jdbc:hsqldb:mem:sss-hsql-ledger"
  user = "SA"
  pass = ""
  createSql = [
    "CREATE TABLE IF NOT EXISTS ledger (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), txid BLOB, entry BLOB);",
    "CREATE TABLE IF NOT EXISTS utxo (txid VARCHAR(64) NOT NULL, indx INT NOT NULL, entry BLOB NOT NULL);",
    "ALTER TABLE utxo ADD PRIMARY KEY (txid, indx);"
  ]

}

akka {
  io {
    tcp {
      direct-buffer-size = 1536 KiB
      trace-logging = off
    }
  }
}