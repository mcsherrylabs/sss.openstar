bob {

  bind {
    port = 7071
    nodeId = "bob"
    declaredAddressOpt = "127.0.0.1"
    bindAddress = "127.0.0.1"
    applicationName = "asado"
    connectionTimeout = 60
    localOnly = false
    connectionRetryIntervalSecs = 10
    appVersion = "1.0.0"
  }

  peers = [ "alice:127.0.0.1:7070", "eve:127.0.0.1:7072" ]


  blockchain {
    inflationRatePerBlock = 1
    maxTxPerBlock = 1000
    maxBlockOpenSecs = 10
  }

  upnphide {
    upnpGatewayTimeoutOpt = 5
    upnpDiscoverTimeoutOpt = 5
  }

  database {
    driver = "org.hsqldb.jdbc.JDBCDriver"
    connection = "jdbc:hsqldb:sss-hsql-ledger-bob"
    useShutdownHook = true
    user = "SA"
    pass = ""
    maxPoolSize = 10
    cachePrepStmts = true
    prepStmtCacheSize = 250
    prepStmtCacheSqlLimit = 2048
    useServerPrepStmts = true
    createSql = [
      "CREATE TABLE IF NOT EXISTS blockchain (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), height BIGINT NOT NULL,    num_txs INT NOT NULL, prev_block BLOB NOT NULL, merkle_root BLOB NOT NULL, mine_dt BIGINT NOT NULL)",
      "CREATE TABLE IF NOT EXISTS utxo (txid VARCHAR(64) NOT NULL, indx INT NOT NULL, entry BLOB NOT NULL, PRIMARY KEY (txid, indx))"

    ]
  }
}

eve {

  peers = ["alice:127.0.0.1:7070", "bob:127.0.0.1:7071"]

  bind {
    nodeId = "eve"
    port = 7072
    declaredAddressOpt = "127.0.0.1"
    bindAddress = "127.0.0.1"
    applicationName = "asado"
    connectionTimeout = 60
    localOnly = false
    connectionRetryIntervalSecs = 10
    appVersion = "1.0.0"
  }


  blockchain {
    inflationRatePerBlock = 1
    maxTxPerBlock = 1000
    maxBlockOpenSecs = 10
  }

  database {
    driver = "org.hsqldb.jdbc.JDBCDriver"
    connection = "jdbc:hsqldb:sss-hsql-ledger-eve"
    user = "SA"
    useShutdownHook = true
    maxPoolSize = 10
    cachePrepStmts = true
    prepStmtCacheSize = 250
    prepStmtCacheSqlLimit = 2048
    useServerPrepStmts = true
    pass = ""
    createSql = [
      "CREATE TABLE IF NOT EXISTS blockchain (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), height BIGINT NOT NULL,    num_txs INT NOT NULL, prev_block BLOB NOT NULL, merkle_root BLOB NOT NULL, mine_dt BIGINT NOT NULL)",
      "CREATE TABLE IF NOT EXISTS utxo (txid VARCHAR(64) NOT NULL, indx INT NOT NULL, entry BLOB NOT NULL, PRIMARY KEY (txid, indx))"

    ]

  }
}

lenny {

  peers = ["alice:127.0.0.1:7070", "bob:127.0.0.1:7071", "eve:127.0.0.1:7072", "karl:127.0.0.1:7074"]

  bind {
    nodeId = "lenny"
    port = 7073
    declaredAddressOpt = "127.0.0.1"
    bindAddress = "127.0.0.1"
    applicationName = "asado"
    connectionTimeout = 60
    localOnly = false
    connectionRetryIntervalSecs = 10
    appVersion = "1.0.0"
  }


  blockchain {
    inflationRatePerBlock = 1
    maxTxPerBlock = 1000
    maxBlockOpenSecs = 10
  }

  database {
    driver = "org.hsqldb.jdbc.JDBCDriver"
    connection = "jdbc:hsqldb:sss-hsql-ledger-lenny"
    user = "SA"
    useShutdownHook = true
    maxPoolSize = 10
    cachePrepStmts = true
    prepStmtCacheSize = 250
    prepStmtCacheSqlLimit = 2048
    useServerPrepStmts = true
    pass = ""
    createSql = [
      "CREATE TABLE IF NOT EXISTS blockchain (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), height BIGINT NOT NULL,    num_txs INT NOT NULL, prev_block BLOB NOT NULL, merkle_root BLOB NOT NULL, mine_dt BIGINT NOT NULL)",
      "CREATE TABLE IF NOT EXISTS utxo (txid VARCHAR(64) NOT NULL, indx INT NOT NULL, entry BLOB NOT NULL, PRIMARY KEY (txid, indx))"

    ]

  }
}

karl {

  peers = ["bob:127.0.0.1:7071"]

  bind {
    nodeId = "karl"
    port = 7074
    declaredAddressOpt = "127.0.0.1"
    bindAddress = "127.0.0.1"
    applicationName = "asado"
    connectionTimeout = 60
    localOnly = false
    connectionRetryIntervalSecs = 10
    appVersion = "1.0.0"
  }


  blockchain {
    inflationRatePerBlock = 1
    maxTxPerBlock = 1000
    maxBlockOpenSecs = 10
  }

  database {
    driver = "org.hsqldb.jdbc.JDBCDriver"
    connection = "jdbc:hsqldb:sss-hsql-ledger-karl"
    user = "SA"
    useShutdownHook = true
    maxPoolSize = 10
    cachePrepStmts = true
    prepStmtCacheSize = 250
    prepStmtCacheSqlLimit = 2048
    useServerPrepStmts = true
    pass = ""
    createSql = [
      "CREATE TABLE IF NOT EXISTS blockchain (id BIGINT GENERATED BY DEFAULT AS IDENTITY (START WITH 1, INCREMENT BY 1), height BIGINT NOT NULL,    num_txs INT NOT NULL, prev_block BLOB NOT NULL, merkle_root BLOB NOT NULL, mine_dt BIGINT NOT NULL)",
      "CREATE TABLE IF NOT EXISTS utxo (txid VARCHAR(64) NOT NULL, indx INT NOT NULL, entry BLOB NOT NULL, PRIMARY KEY (txid, indx))"

    ]

  }
}